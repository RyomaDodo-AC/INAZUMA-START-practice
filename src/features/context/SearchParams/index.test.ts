import * as index from './index'

describe('index', () => {
  it('useSearchParamsContext関数が存在する', () => {
    expect(index.useSearchParamsContext).toBeTruthy()
  })

  it('searchParamsContextDefaultValue関数が存在する', () => {
    expect(index.searchParamsContextDefaultValue).toBeTruthy()
  })

  it('searchParamsContext関数が存在する', () => {
    expect(index.searchParamsContext).toBeTruthy()
  })

  it('useSearchParamsContext関数が存在する', () => {
    expect(index.useSearchParamsContext).toBeTruthy()
  })

  it('SearchParamsProvider関数が存在する', () => {
    expect(index.SearchParamsProvider).toBeTruthy()
  })
})
